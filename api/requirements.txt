fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.0
httpx>=0.27.0

# LangGraph pipeline (OpenAI only — no Gemini/Anthropic)
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
openai>=1.40.0

# Supabase (service-role writes, bypasses RLS)
supabase>=2.6.0

# Gmail SMTP for the auto-email step
aiosmtplib>=3.0.0
